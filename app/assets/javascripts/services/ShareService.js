'use strict';

momentos.factory('ShareService', ['$http', '$q', function($http, $q){

    var getAll = function(){

    };

    var momentService = {
        moments: [],
        getAll: getAll
    }

    return momentService;

}]);
